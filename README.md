1. Install libraries on client/ and on server/

```bash
npm install
```

2. Start application

Using two different terminal, one for both folders.

Client:
```bash
npm run serve
```

Server:
```bash
nodemon
```
